1. 데이터 조회는 반드시 DB에서 필요한 만큼만 가져온다.

2. 아래 작업은 DB에서 처리한다:
   - 필터링 (WHERE)
   - 집계 (GROUP BY)
   - 정렬 (ORDER BY)
   - JOIN

3. 아래 작업은 pandas(DataFrame)에서 처리한다:
   - 파생 컬럼 생성
   - 비율 계산
   - rolling / 통계 처리
   - 시각화용 가공

4. 전체 테이블을 그대로 가져오는 쿼리는 금지한다.

5. DataFrame은 "최종 결과 가공 단계"에서만 사용한다.